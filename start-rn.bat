@echo off
setlocal

set "SCRIPT_DIR=%~dp0"
set "BACKEND_DIR=%SCRIPT_DIR%App\API"
set "RN_DIR=%SCRIPT_DIR%App\NativeAppUI"
set "ENV_FILE=%SCRIPT_DIR%App\.env"
set "RN_CONFIG_FILE=%RN_DIR%\generatedLocalConfig.js"

:: ── Detect local IP ───────────────────────────────────────────────────────────
set "LOCAL_IP="
for /f "tokens=2 delims=:" %%A in ('ipconfig ^| findstr /i "IPv4"') do (
    if not defined LOCAL_IP set "LOCAL_IP=%%A"
)
:: Strip leading space
for /f "tokens=* delims= " %%A in ("%LOCAL_IP%") do set "LOCAL_IP=%%A"

if not defined LOCAL_IP (
    echo.
    echo ERROR: Could not detect a local IP address.
    echo Make sure you are connected to WiFi and try again.
    echo.
    pause
    exit /b 1
)

echo [config] Detected IP: %LOCAL_IP%

:: ── Write App\.env (Flask reads this) ────────────────────────────────────────
(
    echo VITE_LOCAL_DEV=true
    echo VITE_LOCAL_IP=%LOCAL_IP%
    echo LOCAL_IP=%LOCAL_IP%
) > "%ENV_FILE%"

:: ── Write App\NativeAppUI\generatedLocalConfig.js (Expo app reads this) ───────────────
(
    echo // Auto-generated by start-rn.bat -- do not edit.
    echo module.exports = { LOCAL_BASE_URL: 'http://%LOCAL_IP%:5000' };
) > "%RN_CONFIG_FILE%"

echo [config] Written %RN_CONFIG_FILE%

:: ── Launch Flask + Expo ───────────────────────────────────────────────────────
start "Cashflow - Backend" cmd /k "cd /d "%BACKEND_DIR%" && python backend.py"
start "Cashflow - Expo" cmd /k "cd /d "%RN_DIR%" && npx expo start"

echo.
echo Started Backend and Expo.
echo Scan the QR code in the Expo window with Expo Go on your phone.
echo Your phone must be on the same WiFi as this machine (%LOCAL_IP%).
echo.
